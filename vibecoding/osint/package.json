{
  "name": "osint",
  "version": "1.0.0",
  "description": "OSint TUI app",
  "main": "index.ts",
  "scripts": {
    "build": "mkdir -p .build && scriptc build src/index.ts --dynamic --provenance-sources --npm-static auto -o .build/app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.20.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "dependencies": {
    "@opentui/core": "^0.5.4",
    "blessed": "^0.1.81"
  },
  "devDependencies": {
    "@types/blessed": "^0.1.27",
    "@types/node": "^26.2.0"
  }
}
