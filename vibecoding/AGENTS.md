# AGENTS.md

## Agent STRICT Operating Rules

- Preserve architecture. Do not introduce new layers, parallel flows, or public APIs unless explicitly requested
- Stop if a fix conflicts with an ADR, spec, or invariant