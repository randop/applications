{
  "name": "hello",
  "version": "1.0.0",
  "description": "",
  "main": "hello.ts",
  "scripts": {
    "start": "scriptc run hello.ts",
    "build": "mkdir -p .build && scriptc build hello.ts -o .build/hello",
    "start:server": "scriptc run server.ts",
    "build:server": "mkdir -p .build && scriptc build server.ts -o .build/server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.20.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "scriptc": "^0.0.33"
  }
}
